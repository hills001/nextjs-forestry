---
label: Block
hide_body: true
fields:
- name: block_type
  type: select
  default: []
  config:
    required: false
    options:
    - promo
    - card
    - section
    source:
      type: simple
      section: 
      file: 
      path: 
  label: Block type
- name: main_image
  type: file
  config:
    maxSize: 64
  label: Main image
- name: title
  type: text
  config:
    required: false
  label: Title
- name: content
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: false
    schema:
      format: markdown
  label: Content
